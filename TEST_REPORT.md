# V18.3 五项修复与回归报告

## 基线和范围

本版本以 GitHub main 上的 V18.2 `index.html` 为精确基线。其 Git Blob SHA 为：

```text
e5e400d99de2b33e038d164454e7fba91ff5cd63
```

只修改用户指定的五项。V18.2 的 PDF Pencil 连续采样、页面渲染延迟切换、Tile、节点连接和 Storage Worker 未改动。

## 修复摘要

- 字号作为新的内联样式叠加，保留已设置颜色和其他局部格式。
- Tab 在当前光标处插入四个文本空格。
- T 使用画布内最后一个 Pointer 坐标建立文字框。
- 自由文字只手动调整宽度，高度根据内容自动更新；节点保持自然高度。
- 两条悬浮工具栏可从普通表面或按钮区域进行 Mac 三指拖动，7px 阈值避免误触。

滑块、颜色输入和下拉框继续优先处理自身操作。

## 自动检查

- V18.2 本地文件与 GitHub main 的 Blob SHA 一致
- 主应用 JavaScript 语法通过
- Storage Worker JavaScript 语法通过
- 重复 HTML ID：0
- 缺失 DOM 引用：0
- 原压力算法测试通过
- 1000笔 Tile 合成测试通过
- 复杂 PDF 渲染测试通过
- V18.1 回归测试通过
- V18.2 Pointer/PDF 回归测试通过
- V18.3 五项回归测试通过

真实设备仍需验证富文本选区、Mac 系统三指拖动和实际文字换行高度。
