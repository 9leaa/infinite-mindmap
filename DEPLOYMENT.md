# GitHub Pages V15.3 更新

用本包中的文件覆盖现有仓库文件，然后执行：

```bash
git add .
git commit -m "Update to V15.3 tablet tool switching"
git push
```

GitHub Actions 会自动重新部署。

如果浏览器仍显示旧工具栏：

1. 等待 Actions 部署完成。
2. 刷新页面。
3. 仍未更新时，关闭已安装的主屏幕应用后重新打开。
4. 必要时清除该站点的缓存，但不要清除网站数据；清除前先备份全部笔记。
